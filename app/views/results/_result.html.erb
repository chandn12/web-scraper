<%= tag.div id: dom_id(result), class: "d-flex gap-2" do %>
    <div class="w-25 fw-semibold">
        <%= result.success? ? "Success" : "Fail" %>
    </div>
    <div class="fw-light small">
        <%= time_ago_in_words(result.created_at).upcase_first %> ago
    </div>
<% end %>